html
    head
        link(rel="stylesheet", href="/login.css")
            
        title MooVie Database
body
    div#header
        a#home(href="/") Home
        |
        |
        a#account(href="/login") Log in/Sign Up
        |
        |

    div#main
        |  
        form#f(action="/" method="post") 
            p UserName:
                 input(type="textbox" name= "username" required)
            p Password:
                input(type="password" name="password" required)
            button(type="submit" formaction="/login" form="f") Login 
            button(type="submit" formaction="/users/" form="f") Create Account

        if (error) 
            p= error


script(src='/login.js')

